@* criar a view de jogador *@

@model Jogador

@{
    ViewData["Title"] = "Jogador";
}

<body>
    
    <h1>
        Tela de Jogadores
    </h1>

    <form method="post" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
    
        <label for="" >Nome</label>
        <input type="text" placeholder="Digite seu nome..." name="Nome" required>

        <label for="" >Email</label>
        <input type="text" placeholder="Digite seu email..." name="Email" required>

        <label for="" >Senha</label>
        <input type="text"  placeholder="Digite sua senha..." name="Senha" required>
        @*fazer a validação para não aparecer a senha no front-end*@

        <label for="" >IdEquipe</label>
        <input type="text"  placeholder="Digite sua IdEquipe..." name="IdEquipe" required>

        <button type="submit">Cadastrar</button>

    </form>

<table class="table table-striped table-responsive">
    <thead>
        <th>Id</th>
        <th>Nome</th>
        <th>Email</th>
        <th>IdEquipe</th>
        <th>Excluir</th>
        <th>Editar</th>
    </thead>


    <tbody>
        @foreach (Jogador jogador in ViewBag.Jogador)
        {
            <tr>
                <td>@jogador.IdJogador</td>
                <td>@jogador.Nome</td>
                <td>@jogador.Email</td> @* Realizar a criação do menu de escolha de times*@
                <td>@jogador.IdEquipe</td>
                <td>
                    <a asp-controller="Jogador" asp-action="Excluir" asp-route-id="@jogador.IdJogador"> 🗑️ </a>
                </td>
                <td>
                    <a class="nav-link text-dark" asp-area="" asp-controller="Jogador" asp-action="Editar"
                        asp-route-id="@jogador.IdJogador"> ✏️ </a>
                </td>
            </tr>
        }
    </tbody>

</body>